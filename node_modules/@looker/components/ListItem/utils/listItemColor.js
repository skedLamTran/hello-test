import "core-js/modules/es.array.includes.js";
import "core-js/modules/es.string.includes.js";
import { listItemColorAppliesToLabel, listItemColorOptions } from '../types';

var listItemColor = function listItemColor(color, disabled, defaultColor) {
  if (disabled) {
    return 'text1';
  } else if (color) {
    if (listItemColorAppliesToLabel.includes(color)) {
      return color;
    } else if (!listItemColorOptions.includes(color)) {
      return color;
    }
  }

  return defaultColor;
};

export var listItemIconColor = function listItemIconColor(color, disabled) {
  return listItemColor(color, disabled, 'text2');
};
export var listItemLabelColor = function listItemLabelColor(color, disabled) {
  return listItemColor(color, disabled, 'text5');
};
//# sourceMappingURL=listItemColor.js.map
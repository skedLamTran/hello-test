import "core-js/modules/es.array.concat.js";
import { css } from 'styled-components';
import { accordionDimensions } from '../../Accordion2/accordionDimensions';
export var generateIndentCalculation = function generateIndentCalculation(depth, density, theme) {
  var space = theme.space,
      sizes = theme.sizes;

  var _accordionDimensions = accordionDimensions(density),
      indicatorGap = _accordionDimensions.indicatorGap,
      indicatorSize = _accordionDimensions.indicatorSize;

  return "calc((".concat(sizes[indicatorSize], " + ").concat(space[indicatorGap], ") * ").concat(depth, ")");
};
export var generateIndent = function generateIndent(_ref) {
  var _ref$depth = _ref.depth,
      depth = _ref$depth === void 0 ? 0 : _ref$depth,
      density = _ref.density;
  return css(["padding-left:", ";"], function (_ref2) {
    var theme = _ref2.theme;
    return generateIndentCalculation(depth, density || theme.defaults.density, theme);
  });
};
//# sourceMappingURL=generateIndent.js.map
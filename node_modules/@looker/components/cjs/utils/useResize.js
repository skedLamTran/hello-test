"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useResize = void 0;

var _throttle = _interopRequireDefault(require("lodash/throttle"));

var _resizeObserverPolyfill = _interopRequireDefault(require("resize-observer-polyfill"));

var _useSafeLayoutEffect = require("./useSafeLayoutEffect");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const useResize = (element, handler) => {
  (0, _useSafeLayoutEffect.useSafeLayoutEffect)(() => {
    const throttledHandler = (0, _throttle.default)(handler, 100);

    if (!element) {
      return;
    }

    handler();
    const resizeObserver = new _resizeObserverPolyfill.default(() => throttledHandler());

    if (element) {
      resizeObserver.observe(element);
    }

    window.addEventListener('resize', throttledHandler);
    return () => {
      window.removeEventListener('resize', throttledHandler);

      if (resizeObserver) {
        resizeObserver.disconnect();
      }
    };
  }, [handler, element]);
};

exports.useResize = useResize;
//# sourceMappingURL=useResize.js.map
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.listItemLabelColor = exports.listItemIconColor = void 0;

require("core-js/modules/es.array.includes.js");

require("core-js/modules/es.string.includes.js");

var _types = require("../types");

const listItemColor = (color, disabled, defaultColor) => {
  if (disabled) {
    return 'text1';
  } else if (color) {
    if (_types.listItemColorAppliesToLabel.includes(color)) {
      return color;
    } else if (!_types.listItemColorOptions.includes(color)) {
      return color;
    }
  }

  return defaultColor;
};

const listItemIconColor = (color, disabled) => listItemColor(color, disabled, 'text2');

exports.listItemIconColor = listItemIconColor;

const listItemLabelColor = (color, disabled) => listItemColor(color, disabled, 'text5');

exports.listItemLabelColor = listItemLabelColor;
//# sourceMappingURL=listItemColor.js.map
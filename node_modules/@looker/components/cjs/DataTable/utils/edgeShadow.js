"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.edgeShadow = void 0;

var _styledComponents = require("styled-components");

const edgeShadow = (placement = 'left', depth = 4) => {
  let pseudo = ':after';
  let relativeTo = 'right';
  let shadowReverse = '';

  if (placement === 'right') {
    pseudo = ':before';
    relativeTo = 'left';
    shadowReverse = '-';
  }

  const shadow = `${`${shadowReverse}${depth}px`} 0 ${depth}px -${depth}px rgba( 0, 0, 0, 0.25) inset`;
  const position = depth + 7;
  return (0, _styledComponents.css)(["&", "{box-shadow:", ";content:' ';height:100%;position:absolute;", " top:0;width:", "px;}"], pseudo, shadow, `${relativeTo}: -${position}px;`, position);
};

exports.edgeShadow = edgeShadow;
//# sourceMappingURL=edgeShadow.js.map
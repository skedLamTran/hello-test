import { css } from 'styled-components';
import { uiBlends } from '../blendPoints';
import { mixScaledColors } from './mixScaledColors';
export var intentUIBlend = function intentUIBlend(intent, level) {
  return css(["", ""], function (_ref) {
    var colors = _ref.theme.colors;
    return mixScaledColors(uiBlends[level], colors[intent], colors.background);
  });
};
//# sourceMappingURL=intentUIBlend.js.map
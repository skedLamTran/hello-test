import darken from 'polished/lib/color/darken';
import getLuminance from 'polished/lib/color/getLuminance';
import lighten from 'polished/lib/color/lighten';
import { css } from 'styled-components';
export var generateIntentShade = function generateIntentShade(color) {
  var intentColorLuminance = getLuminance(color);
  var adjustAmount = intentColorLuminance > 0.3 ? intentColorLuminance * 0.55 : 0.125;
  return css(["", ""], function (_ref) {
    var colors = _ref.theme.colors;
    return getLuminance(colors.background) > 0.5 ? darken(adjustAmount, color) : lighten(adjustAmount, color);
  });
};
//# sourceMappingURL=generateIntentShade.js.map
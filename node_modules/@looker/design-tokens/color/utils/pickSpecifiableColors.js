import pick from 'lodash/pick';
import { specifiableColors } from '../types';
export var pickSpecifiableColors = function pickSpecifiableColors(colors) {
  return pick(colors, specifiableColors);
};
//# sourceMappingURL=pickSpecifiableColors.js.map
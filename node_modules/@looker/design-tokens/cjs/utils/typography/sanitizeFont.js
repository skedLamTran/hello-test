"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.sanitizeFontFace = exports.sanitizeFontFamilyArray = exports.sanitizeFontFamily = void 0;

require("core-js/modules/es.array.map.js");

require("core-js/modules/es.regexp.exec.js");

require("core-js/modules/es.string.split.js");

require("core-js/modules/es.string.trim.js");

require("core-js/modules/es.string.replace.js");

const sanitizeFontFamily = faces => sanitizeFontFamilyArray(faces).join(', ');

exports.sanitizeFontFamily = sanitizeFontFamily;

const sanitizeFontFamilyArray = faces => faces.split(',').map(face => sanitizeFontFace(face));

exports.sanitizeFontFamilyArray = sanitizeFontFamilyArray;

const sanitizeFontFace = face => {
  const sanitized = face.replace(/["']/g, '').trim();
  return /\s/.test(sanitized) ? `'${sanitized}'` : sanitized;
};

exports.sanitizeFontFace = sanitizeFontFace;
//# sourceMappingURL=sanitizeFont.js.map
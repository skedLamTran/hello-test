import { generateTheme } from '@looker/design-tokens';
import { ThemeContext, ThemeProvider } from 'styled-components';
import React, { useMemo, useContext } from 'react';
export var ExtendComponentsThemeProvider = function ExtendComponentsThemeProvider(_ref) {
  var children = _ref.children,
      themeCustomizations = _ref.themeCustomizations;
  var parentTheme = useContext(ThemeContext);
  var theme = useMemo(function () {
    return generateTheme(parentTheme, themeCustomizations);
  }, [parentTheme, themeCustomizations]);
  return React.createElement(ThemeProvider, {
    theme: theme
  }, children);
};
//# sourceMappingURL=ExtendComponentsProvider.js.map